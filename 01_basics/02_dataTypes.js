"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser  // in browser it will give 6 but in nodejs it will give error

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53 (range of number)
// bigint (for bigger numbers in trading , facebook etc.)
// string => ""
// boolean => true/false
// null => standalone value (means the variable is empty) // null is object
// undefined => (when value is not defined)
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
console.log(typeof "suraj")